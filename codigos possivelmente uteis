codigos que podem me ajudar um dia

simula uma luta entre participantes mas pode ser usado para outra coisa, pegando um participante aleatorio


while (participantes.length > 1) {
      const i1 = Math.floor(Math.random() * participantes.length);
      let i2;
      do {
        i2 = Math.floor(Math.random() * participantes.length);
      } while (i2 === i1);}}


Tem 3 elementos na lista chamada opcoes... 

let opcoes = ["tesoura", "pedra","papel"]
pega um elemento aleatorio da lista criada :)
computadorEscolha = opcoes[Math.floor(Math.random()*opcoes.length)
